# Проект: Браузерная текстово-кнопочная онлайн-игра

## Стек технологий
- **Backend**: .NET 9, C#
- **Архитектура**: CQRS с использованием MediatR, DDD
- **Frontend**: React, TypeScript
- **Стили**: Tailwind CSS
- **База данных**: PostgreSQL
- **State management на фронте**: RTK Query (Redux Toolkit Query)

## Структура проекта
- `/src/Host` — стартовый проект на .NET
- `/src/Host/ClientApp` — клиентская часть на React
- `/src/Domain` — доменный слой
- `/src/Application` — слой Application
- `/src/Infrastructure` — слой Infrastructure

## Конфигурация HTTPS и SSL
- **Используется**: Nginx в качестве reverse-proxy, который обрабатывает HTTPS и перенаправляет HTTP-запросы на внутренний Kestrel-сервер.
- **Сертификаты**: Let's Encrypt, получаемые и обновляемые через Certbot в контейнере Nginx.
- **Конфигурация Nginx**: Файл nginx.conf в корне проекта. Содержит настройки редиректа с HTTP на HTTPS, пути к SSL-сертификатам и прокси-правила для Kestrel.
- **Kestrel**: Работает только на HTTP (порт 80) внутри Docker-сети. Не требует настроек SSL в коде приложения.
- **Порты**:
- **Внешний мир**: HTTPS (443), HTTP (80 — редиректит на HTTPS)
- **Внутренняя сеть**: Kestrel слушает порт 80, Nginx общается с ним по HTTP.
- **В Development**: Локальная разработка использует HTTP и не требует сертификатов. Для локального тестирования с Nginx можно использовать тот же docker-compose.yml с переменной ASPNETCORE_ENVIRONMENT=Development.

## Основные правила разработки
- стараться избегать файлы больше 200 строк (по возможности разбивать)
- стараться избегать методов больше 27 строк (по возможности разбивать)
- стараться избегать больше 12 файлов и папок в одной папке

### Backend
- Все бизнес-операции реализовывать через CQRS (Command/Query + MediatR)
- Валидацию запросов делать через атрибуты
- Обрабатывать ошибки через глобальный middleware (Problem Details, кастомные исключения)
- Для работы с БД использовать Entity Framework Core
- Все эндпоинты — RESTful

### Frontend
- Архитектура — FSD
- Компоненты — функциональные, с хуками
- Стили — Tailwind
- API-запросы через RTK Query
- Типизация — строгая TypeScript (никаких `any` без крайней необходимости)

### База данных
- Все изменения БД — через миграции (Code-First)
- Индексы добавлять с умом (по внешним ключам и часто фильтруемым полям)

### Общее
- Код должен быть читаемым, с осмысленными именами переменных и методов
- Не коммитить секреты (.env, appsettings.Development.json, ключи)
- Перед коммитом — убедиться, что проект компилируется (dotnet build) и фронт собирается (npm run build)
- Логирование — через ILogger на бэке, на фронте — console.log только для разработки

## Текущая стадия проекта
- Начальная разработка (прототип игры)

### Разделение страницы реформ (фронтенд)
- `/me/reforms` — список реформ (`ReformsPage`): карточки `ReformCard` с названием и индикатором доступности, клик ведёт на `/me/reforms/:code`.
- Страница конкретной реформы — `ReformPage`: читает `code` из URL-параметра, грузит `ReformDetails` через `getReform`, отображает через `SlideRenderer` (реформа пока = один слайд). После применения (мутация `setReform`) показывается `ResultSlideRenderer`.

## Ссылки
- Основное описание игры: /GAME_CORE.md
- Подробное описание игры: /GAME_DESIGN.md