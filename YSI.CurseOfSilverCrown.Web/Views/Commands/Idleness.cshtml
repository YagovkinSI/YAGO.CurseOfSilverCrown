@using YSI.CurseOfSilverCrown.Core.ViewModels;
@model YSI.CurseOfSilverCrown.Core.Database.Models.Command

@{
    ViewData["Title"] = "Расходы двора";
    var isOptimized = ViewBag.Optimized as bool?;
    var organization = ViewBag.Organization as OrganizationInfo;
}

<h1>Редактирование</h1>

<h4>Расходы двора</h4>
<hr />

@{
    if (isOptimized == null || isOptimized.Value)
    {
        <p>Расходы казны на содержание двора оптимизированы и утверждены: @Html.DisplayFor(model => model.Coffers)</p>
    }
    else
    {
        <p>Расходы казны на содержание двора: @Html.DisplayFor(model => model.Coffers)</p>
        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id" asp-route-optimizeIdleness="true">Оптимизировать расходы и утвердить</a>
    }
}

<div class="row">
    <p>
        Расходы двора - это всевозможные расходы, за исключением расходов на содержание воинов. В расходы двора входят ремонтные работы защитных укреплений, пиры, турниры, дорогие украшения придворных и многое другое. Расточительные правители не занимаются проверкой трат, что может приводить к лишним потерям в казне и коррупции. Каждый сезон Вы можете провести оптимизацию расходов и уменьшить предлагаемые траты.
    </p>
</div>

<div>
    <a asp-action="Index" asp-route-organizationId="@organization.Id">К списку</a>
</div>
